# AI 네이티브 프로젝트 계약 초기 설계

이 문서는 AI 에이전트에게 프로그램 개발을 오래 맡기기 전에, 어떤 프로젝트 계약 문서를 만들고 어떤 결정을 고정해야 하는지 정리한다.

목표는 코드를 빨리 많이 만드는 것이 아니다.

목표는 다음과 같다.

```text
에이전트가 구현 중 길을 잃지 않게 하기
작업자가 바뀌어도 같은 방향으로 이어서 개발하게 하기
범위 폭주와 과설계를 막기
완료 기준과 검증 방법을 먼저 정하기
문서와 코드가 어긋났을 때 멈추고 고치게 하기
```

AI 네이티브 개발에서는 초기 설계 문서가 사실상 프로젝트의 운영체제다.

---

# 핵심 원칙

## 1. 구현보다 지시문서가 먼저다

처음부터 코드를 작성하게 하면 에이전트는 그럴듯한 구조를 빠르게 만든다.

문제는 그 구조가 실제 목표, 검증 방법, 장기 유지보수 방향과 맞는지 확인하기 어렵다는 것이다.

구현 전에 목표와 범위 밖, 책임 경계, 사용자 결정 항목, verification gate를 프로젝트 문서에 고정한다.

그 뒤의 현재·다음 범위와 의존 관계는 `docs/implementation-plan.md`에 기록한다. 이 문서는 모든 프로젝트에 같은 구현 순서를 정하지 않는다.

## 2. 문서는 많을수록 좋은 것이 아니다

좋은 문서는 에이전트의 판단 비용을 줄인다.

나쁜 문서는 같은 규칙을 여러 곳에 흩뿌려서 충돌을 만든다.

문서를 만들 때는 각 문서가 어떤 단일 출처인지 먼저 정한다.

```text
프로젝트 규칙의 단일 출처
아키텍처 경계의 단일 출처
현재와 다음 범위의 단일 출처
검증 기준의 단일 출처
PR 설명 기준의 단일 출처
중요한 결정과 이유의 단일 출처
```

프로젝트에 필요한 계약만 선택하며, 이 목록을 고정된 파일 목록으로 해석하지 않는다.

## 3. "아직 하지 않을 것"을 반드시 적는다

에이전트는 빈 공간을 보면 채우려 한다.

그래서 초기 설계에는 하지 않을 일을 명시해야 한다.

예:

```text
아직 하지 않는다:
- 플러그인 시스템
- 설정 UI
- 멀티 플랫폼 지원
- 완전한 최적화
- 전체 프로토콜 호환성
```

이 목록은 게으름이 아니라 범위 제어 장치다.

## 4. 모르면 진행하지 말아야 할 결정을 구분한다

에이전트에게 모든 결정을 맡기면 빠르지만, 제품 방향이 쉽게 틀어진다.

문서에는 사용자 결정이 필요한 영역을 따로 둔다.

```text
사용자 결정 필요:
- 핵심 UX 방향
- 공개 API 호환성
- 새 의존성
- 라이선스 위험이 있는 reference 사용
- 보안/개인정보 정책
- 장기 구조를 바꾸는 아키텍처 결정
```

같은 질문을 여러 문서에 복사하지 않는다. `AGENTS.md`는 현재 작업을 막는 전역 질문의 index로 쓰고, 세부 질문은 관련 계약을 소유한 문서에 둔다. 장기적이거나 여러 영역에 영향을 주는 보류 결정이 있어 `docs/decisions.md`를 추가했다면 그 문서가 해당 질문을 소유한다.

## 5. 완료는 "구현됨"이 아니라 "증명됨"이다

AI 에이전트에게 일을 맡길수록 완료 기준이 중요하다.

완료 기준은 다음을 포함해야 한다.

```text
어떤 테스트를 통과했는가
어떤 산출물이 남는가
자동 검증이 안 되는 부분은 무엇인가
수동 검증은 어떻게 했는가
남은 한계는 무엇인가
문서와 코드가 같은 상태를 설명하는가
```

---

# 계약 문서 선택

새 프로젝트에 복사할 실제 템플릿은 `project-documents-template.md`가 단일 출처다. 모든 문서를 기본으로 만들지 않고, 현재 프로젝트가 보존해야 할 계약에 맞춰 선택한다.

| 계층 | 문서 | 선택 조건 |
| --- | --- | --- |
| 최소 | `AGENTS.md`, `docs/implementation-plan.md` | 작은 실험에서 목표, 중단 조건, 현재 범위와 검증을 두 문서로 관리할 수 있다 |
| 표준 | `docs/project-rules.md`, `docs/architecture.md`, `docs/verification-matrix.md` | 여러 책임 영역이나 세션에서 공통 규칙, 경계, 검증 기준을 독립된 단일 출처로 유지해야 한다 |
| 조건부 | `docs/pr-checklist.md` | 반복되는 완료 보고 형식이나 PR gate가 필요하다 |
| 조건부 | `docs/decisions.md`, `docs/rfcs/` | 되돌리기 어렵거나 여러 영역에 영향을 주는 결정이 있다 |
| 조건부 | 기능·artifact·성능 문서 | 기존 문서로 소유권을 명확히 표현할 수 없다 |

표준 구성은 최소 구성에 해당 문서를 추가하는 누적형이다.

빈 문서를 미리 만들지 않는다. 조건부 문서를 추가할 때는 기존 문서에 둘 수 없는 이유와 새 문서가 맡을 단일 출처를 함께 정한다.

# 초기 프롬프트 템플릿

새 프로젝트를 시작할 때 에이전트에게 바로 줄 수 있는 프롬프트다.

```text
나는 <프로그램 설명>을 만들고 싶다.

아직 코드를 작성하지 마라.
먼저 AI 에이전트와 방법론이 바뀌어도 프로젝트의 의도와 제약이 유지되도록
프로젝트 계약 문서를 설계해라.

목표:
- 구현자가 바뀌어도 같은 방향으로 작업할 수 있어야 한다.
- 목표와 범위 밖, 사람이 결정할 경계가 명확해야 한다.
- architecture와 책임 경계가 명확해야 한다.
- 완료 기준이 verification gate나 artifact와 연결되어야 한다.
- 문서와 코드가 어긋났을 때의 처리 기준과 인계 위치가 있어야 한다.

작성 규칙:
- project-documents-template.md에서 프로젝트 규모에 필요한 문서만 선택한다.
- 각 문서의 단일 출처 역할을 정하고 같은 계약을 복사하지 않는다.
- 조건부 문서는 실제 필요 조건이 있을 때만 추가한다.
- coding, debugging, review, task dispatch 방법은 규정하지 않는다.
- 불확실한 결정은 "사용자 결정 필요"로 표시한다.

결과물:
1. 추천 문서 구조와 선택 이유
2. 각 문서가 소유할 계약
3. 실제 파일 초안
4. 사용자 결정이 필요한 질문
```

# 제품 설명에 포함할 입력

문서가 빈칸을 임의로 채우지 않게 최소한 다음을 제공한다.

```text
한 문장 설명
주 사용자
반드시 되는 첫 번째 작업 흐름
이번 범위에서 하지 않을 기능
선호하거나 금지하는 기술 선택
배포 대상
성능, 보안, 개인정보 요구사항
배포물에 포함하도록 승인할 제3자 코드나 자산과 재배포 조건
참고할 제품이나 공개 명세
동작만 참고하고 코드 표현을 가져오면 안 되는 reference-only 대상
```

# 초안 확인

초안을 받은 뒤 다음만 확인한다.

```text
에이전트가 처음 읽을 진입점이 있는가?
각 계약의 단일 출처가 하나인가?
목표, 범위 밖, 책임 경계와 사용자 결정 항목이 드러나는가?
완료 기준이 실행 가능한 gate나 관찰 가능한 evidence에 연결되는가?
다른 agent나 methodology로 바꿔도 계약의 의미가 유지되는가?
현재 필요하지 않은 빈 문서나 보편적 작업 절차가 추가되지 않았는가?
```

세부 형식은 `project-documents-template.md`, 장기 작업의 권한과 인계 기준은 `agent-operating-protocol.md`를 따른다.

# 기억할 원칙

```text
AI에게 코드를 맡기기 전에 프로젝트가 보존할 판단 기준을 문서에 맡긴다.
좋은 계약은 에이전트가 하지 말아야 할 일을 스스로 멈추게 한다.
완료는 구현 여부가 아니라 검증된 상태다.
```
