# AI 네이티브 개발 초기 설계 단계

이 문서는 AI 에이전트에게 프로그램 개발을 오래 맡기기 전에, 처음 어떤 문서를 만들고 어떤 결정을 고정해야 하는지 정리한다.

목표는 코드를 빨리 많이 만드는 것이 아니다.

목표는 다음과 같다.

```text
에이전트가 구현 중 길을 잃지 않게 하기
작업자가 바뀌어도 같은 방향으로 이어서 개발하게 하기
범위 폭주와 과설계를 막기
완료 기준과 검증 방법을 먼저 정하기
문서와 코드가 어긋났을 때 멈추고 고치게 하기
```

AI 네이티브 개발에서는 초기 설계 문서가 사실상 프로젝트의 운영체제다.

---

# 핵심 원칙

## 1. 구현보다 지시문서가 먼저다

처음부터 코드를 작성하게 하면 에이전트는 그럴듯한 구조를 빠르게 만든다.

문제는 그 구조가 실제 목표, 검증 방법, 장기 유지보수 방향과 맞는지 확인하기 어렵다는 것이다.

초기에는 다음 순서를 지킨다.

```text
제품 설명
-> 지시문서 세트
-> 초기 세로 슬라이스
-> 검증
-> 반복 구현
```

## 2. 문서는 많을수록 좋은 것이 아니다

좋은 문서는 에이전트의 판단 비용을 줄인다.

나쁜 문서는 같은 규칙을 여러 곳에 흩뿌려서 충돌을 만든다.

문서를 만들 때는 각 문서가 어떤 단일 출처인지 먼저 정한다.

```text
프로젝트 규칙의 단일 출처
아키텍처 경계의 단일 출처
구현 순서의 단일 출처
검증 기준의 단일 출처
PR 설명 기준의 단일 출처
```

## 3. "아직 하지 않을 것"을 반드시 적는다

에이전트는 빈 공간을 보면 채우려 한다.

그래서 초기 설계에는 하지 않을 일을 명시해야 한다.

예:

```text
아직 하지 않는다:
- 플러그인 시스템
- 설정 UI
- 멀티 플랫폼 지원
- 완전한 최적화
- 전체 프로토콜 호환성
```

이 목록은 게으름이 아니라 범위 제어 장치다.

## 4. 모르면 진행하지 말아야 할 결정을 구분한다

에이전트에게 모든 결정을 맡기면 빠르지만, 제품 방향이 쉽게 틀어진다.

문서에는 사용자 결정이 필요한 영역을 따로 둔다.

```text
사용자 결정 필요:
- 핵심 UX 방향
- 공개 API 호환성
- 새 런타임 의존성
- 라이선스 위험이 있는 reference 사용
- 보안/개인정보 정책
- 장기 구조를 바꾸는 아키텍처 결정
```

## 5. 완료는 "구현됨"이 아니라 "증명됨"이다

AI 에이전트에게 일을 맡길수록 완료 기준이 중요하다.

완료 기준은 다음을 포함해야 한다.

```text
어떤 테스트를 통과했는가
어떤 산출물이 남는가
자동 검증이 안 되는 부분은 무엇인가
수동 검증은 어떻게 했는가
남은 한계는 무엇인가
문서와 코드가 같은 상태를 설명하는가
```

---

# 초기 설계 단계의 산출물

작은 프로젝트라면 모든 문서가 필요하지 않을 수 있다.

하지만 AI 에이전트에게 장기 작업을 맡길 계획이라면 아래 문서 세트를 기본값으로 삼는다.

```text
AGENTS.md
docs/project-rules.md
docs/architecture.md
docs/implementation-plan.md
docs/verification-matrix.md
docs/pr-checklist.md
```

## 1. AGENTS.md

에이전트가 가장 먼저 읽는 진입점이다.

여기에 모든 규칙을 길게 쓰지 않는다.

역할:

```text
읽어야 할 문서 순서
각 문서가 맡는 단일 출처
절대 규칙 몇 개
모르면 어디를 봐야 하는지
```

좋은 형태:

```md
# AGENTS.md

## 먼저 읽을 문서

- docs/project-rules.md
- docs/architecture.md
- docs/implementation-plan.md
- docs/verification-matrix.md
- docs/pr-checklist.md

## 원칙

- 구현 전에 관련 문서를 확인한다.
- 문서와 코드가 달라지면 같은 작업에서 맞춘다.
- 문서에 없는 아키텍처 결정을 임의로 하지 않는다.
- 불확실하면 사용자에게 보고한다.
```

## 2. docs/project-rules.md

프로젝트 전체의 작업 규칙이다.

포함할 것:

```text
언어와 런타임 버전
의존성 추가 정책
코딩 스타일
파일 분리 원칙
테스트 원칙
보안/민감정보 정책
문서 정합성 규칙
커밋/PR 규칙
```

특히 중요한 규칙:

```text
새 의존성은 사용 전 이유를 적는다.
관련 문서를 갱신하지 않은 구현은 완료가 아니다.
임시 패치는 왜 임시인지와 나중에 어떻게 제거할지 적는다.
자동 검증이 불가능하면 수동 검증 방법과 한계를 적는다.
```

## 3. docs/architecture.md

전체 구조와 책임 경계를 고정한다.

코드 상세보다 다음이 중요하다.

```text
주요 모듈
각 모듈의 책임
각 모듈이 몰라야 하는 것
데이터 흐름
저장되는 상태와 저장되면 안 되는 상태
```

좋은 아키텍처 문서는 에이전트가 "이 기능을 어디에 넣어야 하는가"를 판단하게 해준다.

예:

```text
Core
  - 도메인 로직
  - 플랫폼 API를 모른다

Runtime
  - live process와 domain object를 연결한다
  - 저장 포맷을 모른다

UI
  - 상태를 표시하고 입력을 전달한다
  - core private storage를 직접 만지지 않는다
```

## 4. docs/implementation-plan.md

구현 순서를 정한다.

좋은 구현 계획은 기능 목록이 아니라, 단계별 학습/검증 경로다.

각 단계에 포함할 것:

```text
목표
구현할 것
아직 하지 않을 것
테스트 방식
완료 기준
문서 갱신 범위
```

좋은 단계 예:

```text
1단계: 최소 facade와 책임 경계 고정
2단계: 실패 산출물과 snapshot 포맷 먼저 만들기
3단계: 가장 작은 end-to-end 경로 구현
4단계: 실제 입력/출력 연결
5단계: UI 또는 배포 경로 연결
```

피해야 할 단계:

```text
1단계: 전체 기능 구현
2단계: 최적화
3단계: 리팩토링
```

너무 넓어서 에이전트가 완료를 판단할 수 없다.

## 5. docs/verification-matrix.md

기능 영역별 검증 방법을 적는다.

가장 중요한 문서 중 하나다.

형식:

```md
| 영역 | 검증 방법 | 산출물 | 의미 | 한계 |
| --- | --- | --- | --- | --- |
| 설정 로딩 | unit test | 없음 | 우선순위 merge 검증 | 실제 파일 watch는 아님 |
| CLI 실행 | integration test | stdout/stderr fixture | 사용자가 보는 동작 검증 | OS별 shell 차이는 별도 |
| 렌더링 | screenshot smoke | artifacts/*.png | 실제 화면 확인 | pixel-perfect 아님 |
```

이 문서는 에이전트가 기능을 끝낼 때 스스로 묻게 만든다.

```text
내가 바꾼 기능은 어느 검증 영역에 연결되는가?
자동 검증이 없으면 왜 없는가?
실패하면 어떤 산출물을 보고 원인을 찾는가?
```

## 6. docs/pr-checklist.md

작업 완료 보고의 기준이다.

PR을 쓰지 않는 개인 프로젝트라도 필요하다.

에이전트의 최종 보고가 이 문서를 따르게 만들면 된다.

포함할 항목:

```text
의도
구현
문서 정합성
테스트
artifact
한계
사용자 결정 필요 여부
후속 작업
```

좋은 체크리스트는 에이전트가 허술하게 "완료했습니다"라고 말하지 못하게 한다.

---

# 초기 프롬프트 템플릿

새 프로젝트를 시작할 때 에이전트에게 바로 줄 수 있는 프롬프트다.

```text
나는 <프로그램 설명>을 만들고 싶다.

아직 코드를 작성하지 마라.
먼저 AI 에이전트가 이 프로젝트를 장기간 자동으로 개발할 수 있도록
초기 지시문서 세트를 설계하고 작성해라.

목표:
- 구현자가 바뀌어도 같은 방향으로 작업할 수 있어야 한다.
- 범위 폭주를 막아야 한다.
- 어떤 결정을 임의로 하면 안 되는지 명확해야 한다.
- 각 기능의 완료 기준과 검증 방법이 있어야 한다.
- 문서와 코드가 어긋났을 때 처리 규칙이 있어야 한다.

먼저 제안할 문서:
- AGENTS.md
- docs/project-rules.md
- docs/architecture.md
- docs/implementation-plan.md
- docs/verification-matrix.md
- docs/pr-checklist.md

작성 규칙:
- 프로젝트 규모에 맞게 최소 문서만 제안해라.
- 각 문서가 왜 필요한지 설명하고, 불필요하면 만들지 마라.
- 불확실한 결정은 "사용자 결정 필요"로 표시해라.
- 각 문서는 단일 출처 역할을 명확히 해라.
- 같은 규칙을 여러 문서에 중복해서 흩뿌리지 마라.
- "아직 하지 않을 것"을 명시해라.
- 검증 불가능한 영역은 수동 검증 방법과 한계를 적어라.

결과물:
1. 추천 문서 구조
2. 각 문서의 역할
3. 실제 파일 초안
4. 사용자가 결정해야 할 질문 목록
```

---

# 제품 설명을 줄 때 포함할 것

에이전트에게 만들고 싶은 프로그램을 설명할 때는 아래 정보를 준다.

```text
1. 한 문장 설명
2. 주 사용자
3. 반드시 되는 첫 번째 작업 흐름
4. 나중에 할 기능
5. 절대 하지 않을 기능
6. 선호 기술 스택
7. 배포 대상
8. 성능/보안/개인정보 요구사항
9. 참고하고 싶은 제품
10. 참고만 하고 베끼면 안 되는 제품/코드
```

예:

```text
한 문장:
  로컬 파일을 읽어 개인 지식베이스를 만들고, 자연어로 검색하는 데스크톱 앱.

첫 번째 작업 흐름:
  사용자가 폴더를 선택한다.
  앱이 Markdown 파일을 인덱싱한다.
  사용자가 질문을 입력한다.
  관련 문서 목록과 인용 위치를 보여준다.

아직 하지 않을 것:
  클라우드 동기화
  멀티 유저
  모바일 앱
  플러그인 시스템

사용자 결정 필요:
  임베딩을 로컬 모델로 할지 API로 할지
  검색 인덱스를 암호화할지
```

---

# 좋은 지시문서의 체크리스트

문서 초안을 받은 뒤 아래 질문으로 리뷰한다.

```text
에이전트가 처음 읽을 진입점이 있는가?
각 문서의 단일 출처 역할이 명확한가?
같은 규칙이 여러 곳에 중복되어 있지 않은가?
아키텍처 경계가 "책임"과 "몰라야 하는 것"으로 적혀 있는가?
구현 계획에 "아직 하지 않을 것"이 있는가?
각 단계의 완료 기준이 테스트/산출물과 연결되어 있는가?
검증 불가능한 영역의 수동 확인 방법이 있는가?
사용자 결정이 필요한 질문이 따로 모여 있는가?
새 의존성, 보안, 개인정보, 라이선스에 대한 규칙이 있는가?
문서와 코드가 달라졌을 때의 처리 규칙이 있는가?
```

하나라도 없으면 구현 전에 문서를 고친다.

---

# 에이전트가 자주 실패하는 지점

## 과설계

증상:

```text
아직 사용자 작업 흐름도 없는데 plugin system, cloud sync, theme engine부터 만든다.
```

방지:

```text
초기 세로 슬라이스를 정한다.
"아직 하지 않을 것"을 단계마다 적는다.
새 의존성은 사용자 확인 대상으로 둔다.
```

## 구현과 문서 불일치

증상:

```text
문서에는 A라고 되어 있는데 코드는 B로 구현되어 있다.
```

방지:

```text
PR 체크리스트에 문서 정합성을 필수 항목으로 둔다.
구현이 문서와 달라져야 하면 먼저 보고하게 한다.
```

## 완료 기준 부재

증상:

```text
"구현했습니다"라고 하지만 실제로 어떤 테스트와 산출물이 있는지 모른다.
```

방지:

```text
verification-matrix를 만든다.
완료 보고에 실행한 명령과 결과를 반드시 적게 한다.
```

## 임의 결정

증상:

```text
에이전트가 라이브러리, 저장 포맷, API 형태를 마음대로 정한다.
```

방지:

```text
사용자 결정 필요 영역을 문서에 둔다.
문서에 없는 아키텍처 결정은 멈추고 질문하게 한다.
```

## 레퍼런스 베끼기

증상:

```text
참고 프로젝트의 구조, 이름, 코드 흐름을 그대로 가져온다.
```

방지:

```text
reference 사용 정책을 정한다.
허용: 동작 비교, 공개 명세, UX 관찰
금지: 코드 표현 복사, 라이선스 위험 소스 차용
```

---

# 최소 버전

작은 프로젝트라면 이 정도만으로 시작한다.

```text
AGENTS.md
docs/project-rules.md
docs/implementation-plan.md
docs/verification-matrix.md
```

더 작은 실험이면 하나의 `AGENTS.md`에 아래 섹션만 둬도 된다.

```text
목표
하지 않을 것
구현 순서
검증 명령
사용자 결정 필요
완료 보고 형식
```

문서 체계는 프로젝트 크기에 맞춰야 한다.

AI 네이티브 개발에서 중요한 것은 문서의 양이 아니라, 에이전트가 다음 행동을 안전하게 결정할 수 있는 정도다.

---

# 기억할 문장

```text
AI에게 코드를 맡기기 전에 판단 기준을 맡길 문서를 만든다.
```

```text
좋은 지시문서는 에이전트가 더 많은 일을 하게 만드는 문서가 아니라,
하지 말아야 할 일을 스스로 멈추게 만드는 문서다.
```

```text
완료는 구현이 아니라 검증된 상태다.
```
