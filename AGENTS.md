# AGENTS.md

## 범위

이 저장소는 AI 네이티브 개발(AI-native development)을 위한 방법론 중립적 프로젝트 계약 템플릿을 담는다.

목표는 사용하는 agent나 methodology가 바뀌어도 프로젝트의 목표, 경계, evidence와 인계 기준이 유지되게 하는 것이다.

## 작업 경계

- 분석, 평가, 리뷰 요청은 변경 요청으로 해석하지 않는다. 파일 수정은 명시적으로 요청받았을 때만 한다.
- 이 저장소를 수정하기 전에 `repo-review-checklist.md`를 확인한다.
- `README.md`에서 문서 역할을 확인하고 작업과 관련된 문서만 읽는다.

## 리뷰와 분석

- 리뷰, 분석, 개선 제안 요청에서는 변경안을 만들어내는 것보다 정확한 판단을 우선한다.
- 반영할 가치가 있는 변경이 없으면 사소한 문구 수정, 불필요한 새 문서, 형식적인 변경을 억지로 제안하거나 적용하지 않는다.
- "반영할 변경 없음"은 유효한 결론이다. 관련 기준을 확인한 결과 현재 상태를 유지하는 편이 더 낫다는 뜻으로 사용한다.

## 편집 원칙

- 추상 이론보다 프로젝트별 값, 경계, evidence를 직접 채울 수 있는 템플릿과 체크리스트를 우선한다.
- 문서는 형식적인 조직 프로세스 문서가 아니라 개인 엔지니어링 노트처럼 읽히게 유지한다.
- 요청을 해결하는 최소 변경만 적용하고, 관련 없는 문구 정리나 문서 전체 재작성은 섞지 않는다.
- 실제 에이전트 실패 모드를 줄이는 구조가 아니라면 문서 체계를 더 무겁게 만들지 않는다.
- 새 규칙을 판단할 때 먼저 "에이전트에게 일을 수행하는 방법을 가르치는가, 아니면 프로젝트가 지속적으로 보존해야 할 사실과 경계를 정의하는가?"를 묻는다. 전자라면 소유 범위 밖이고, 후자도 아래 계약 범위에 해당할 때만 반영한다.
- 이 저장소가 소유하는 규칙은 권한·안전의 불변 조건과 프로젝트가 채워야 하는 목표, 경계, artifact, verification gate, 인계 계약으로 제한한다.
- 에이전트의 coding, debugging, review, task dispatch 순서나 방법은 일반화하지 않는다.
- 일반화할 수 있고 실패 모드를 줄인다는 사실만으로는 반영을 정당화하지 않는다.
- 문서를 개선할 때는 에이전트가 혼자 결정하면 안 되는 항목을 명확히 한다.
- 어떤 artifact를 만들어야 하는지, 완료를 어떻게 검증해야 하는지, 무엇이 범위 밖인지 명확히 한다.
- 의도적으로 중심 규칙으로 두는 경우가 아니라면 같은 규칙을 여러 파일에 중복하지 않는다.
- 두 문서가 충돌하면 조용히 하나를 선택하지 말고 충돌을 보고한다.

## 개선 기준

좋은 개선은 에이전트가 다음을 더 쉽게 하게 만든다.

- 프로젝트 목표 이해
- 허가되지 않은 아키텍처 결정 회피
- 프로젝트별 범위와 검증 기준 이해
- 불확실성 명확히 보고
- 다음 세션을 위한 유용한 맥락 남기기

## 참고 프로젝트 분석

참고 프로젝트(reference project)를 바탕으로 이 문서들을 개선할 때는 다음을 따른다.

- 참고 프로젝트를 새 workflow의 공급원이 아니라 현재 계약 체계의 누락을 점검하는 입력으로 사용한다.
- 참고 프로젝트에 규칙이 존재하거나 잘 작동한다는 사실은 이 저장소의 실패 근거로 보지 않는다.
- 먼저 보완할 기존 문서와 계약을 식별한다. 대응하는 기존 계약이 없으면 기본적으로 반영하지 않는다.
- 새 규칙은 기존 규칙을 대체하거나 단순화하는 경우를 우선한다.
- 새로운 핵심 책임이 필요하면 반복해서 확인된 고비용 실패 근거와 함께 저장소 역할 확장을 사용자 결정으로 보고한다.
- 특정 스택을 다루는 문서가 아니라면 언어, framework, package manager, build tool을 추천으로 추가하지 않는다.
- 특정 agent, methodology, coding/debugging/review 절차를 이름만 바꿔 프로젝트 계약으로 재분류하지 않는다.
- 반영 여부가 애매하면 반영하지 않는다.

## Git

- 명시적으로 요청받지 않으면 commit, amend, rebase, force-push, push를 하지 않는다.
- commit message는 기존 history에 맞춰 영어 conventional-commit 형식을 사용한다.

## 안전

- 예시와 template에는 실제 secret, credential, token, private key를 넣지 않고 placeholder를 사용한다.

## 용어

- 설명 문장은 한국어로 쓴다.
- 코드, 명령, 파일 경로, commit message, config snippet, identifier는 영어로 유지한다.
- 실무에서 영어로 말하는 용어는 영어로 유지한다. 예: `artifact`, `fixture`, `snapshot`, `schema`, `runtime`, `framework`, `package manager`.
- 도구나 제품의 고유 개념은 원래 용어를 유지한다. 예: `GitHub Actions workflow`, `CI workflow`, `workflow file`.
- 헷갈릴 수 있는 핵심 개념만 각 문서의 첫 등장에 한국어와 영어를 병기한다. 예: 인계(handoff), 범위(scope), 범위 밖(non-goals), 결정 기록(decision record).
- 한 문서 안에서는 같은 개념을 같은 표현으로 부른다.

## 하지 않을 것

- 일반적인 AI 생산성 조언을 추가하지 않는다.
- 동기부여식 문장을 추가하지 않는다.
- 템플릿을 긴 에세이로 바꾸지 않는다.
- 문서를 짧게 만들기 위해 필요한 제약을 제거하지 않는다.
