# AI 에이전트 운영 프로토콜

이 문서는 AI 에이전트가 프로젝트를 자동으로 진행할 때 따를 운영 규칙을 정리한다.

좋은 프로젝트 문서가 있어도, 에이전트가 언제 읽고, 언제 멈추고, 어떻게 보고할지 정해져 있지 않으면 자동 작업은 쉽게 흐트러진다.

---

# 기본 루프

에이전트 작업은 항상 아래 루프를 따른다.

```text
목표 확인
-> 관련 문서 읽기
-> 현재 코드/상태 확인
-> 작은 계획 수립
-> 구현
-> 검증
-> 문서 정합성 확인
-> 결과 보고
```

중간에 문서에 없는 결정이 필요하면 루프를 멈추고 사용자에게 보고한다.

---

# 작업 시작 절차

에이전트는 작업 시작 전에 다음을 확인한다.

```text
1. AGENTS.md
2. 작업과 관련된 docs 문서
3. git status
4. 최근 변경 diff
5. 관련 테스트/검증 명령
```

작업 시작 보고는 짧게 한다.

```text
<기능>을 구현하기 전에 <문서>와 <파일>을 확인하겠습니다.
목표는 <완료 기준>까지 검증하는 것입니다.
```

---

# 진행해도 되는 경우

다음 조건을 만족하면 에이전트가 사용자에게 매번 묻지 않고 진행해도 된다.

```text
요청 범위가 명확하다.
관련 문서에 책임 경계가 있다.
새 의존성을 추가하지 않는다.
공개 API나 저장 포맷을 바꾸지 않는다.
보안/개인정보 정책에 영향을 주지 않는다.
검증 방법이 있다.
```

---

# 반드시 멈춰야 하는 경우

다음 상황에서는 구현을 계속하지 말고 사용자에게 보고한다.

```text
문서에 없는 아키텍처 결정을 해야 한다.
새 런타임 의존성이 필요하다.
공개 API, 저장 포맷, 프로토콜, DB schema를 바꿔야 한다.
보안/개인정보/권한 정책이 바뀐다.
기존 전략과 충돌한다.
자동 검증이 불가능하고 수동 검증도 애매하다.
참고 프로젝트의 코드 표현을 가져오고 싶어진다.
요청을 만족하려면 범위를 크게 넓혀야 한다.
```

보고 형식:

```text
멈춘 이유:
  <무엇이 문서에 없거나 위험한가>

선택지:
  1. <안전한 선택>
  2. <범위가 넓은 선택>

추천:
  <추천과 이유>
```

---

# 계획 작성 기준

계획은 길지 않아도 된다.

하지만 다음은 들어가야 한다.

```text
1. 바꿀 책임 영역
2. 건드리지 않을 영역
3. 검증 명령
4. 문서 갱신 여부
```

예:

```text
계획:
1. config loader에 새 옵션 파싱을 추가한다.
2. runtime 동작은 바꾸지 않는다.
3. unit test와 config fixture를 추가한다.
4. docs/configuration.md와 검증 매트릭스(verification matrix)를 갱신한다.
```

---

# 구현 중 상태 보고

긴 작업에서는 30분 단위가 아니라, 의미 있는 경계마다 보고한다.

좋은 보고:

```text
문서와 현재 구현을 확인했습니다.
이 기능은 runtime이 아니라 config 책임입니다.
다음으로 loader test를 먼저 추가하겠습니다.
```

나쁜 보고:

```text
작업 중입니다.
거의 다 됐습니다.
```

상태 보고는 사용자가 판단할 정보를 줘야 한다.

---

# 검증 규칙

작업 완료 전 최소 확인:

```text
관련 단위 테스트
관련 통합/E2E 테스트
포맷/린트
git diff
문서 정합성
```

검증하지 못한 경우 실패가 아니라 정보다.

반드시 보고한다.

```text
실행하지 못한 검증:
  <명령>

이유:
  <환경, 시간, 외부 의존성>

대체 확인:
  <수동 확인 또는 낮은 단계 테스트>

남은 위험:
  <무엇이 아직 불확실한가>
```

---

# 문서 정합성 규칙

다음 중 하나라도 해당하면 문서를 갱신한다.

```text
새 기능이 추가됐다.
명령이 바뀌었다.
테스트 경로가 바뀌었다.
파일 구조가 바뀌었다.
아키텍처 경계가 바뀌었다.
한계나 수동 검증 방법이 새로 생겼다.
```

문서를 갱신하지 않는다면 이유를 완료 보고에 적는다.

---

# 완료 보고 형식

에이전트의 최종 보고는 다음 형식을 따른다.

```text
완료:
  <무엇을 했는가>

변경:
  - <파일/영역>
  - <파일/영역>

검증:
  - <명령>: 통과
  - <명령>: 통과

문서:
  - <갱신한 문서>
  - 문서 변경 없음: <이유>

한계:
  - <없으면 없음>

후속:
  - <필요하면>
```

---

# Handoff 요약 형식

작업이 길어져 다른 에이전트가 이어받을 수 있으면 인계 요약(handoff summary)을 남긴다.

```text
현재 목표:
  <무엇을 끝내려는가>

완료한 것:
  - <완료 항목>

남은 것:
  - <남은 항목>

중요한 결정:
  - <결정과 이유>

주의할 점:
  - <건드리면 안 되는 것>

검증 상태:
  - <통과한 명령>
  - <아직 못 한 명령>

다음 추천 작업:
  1. <다음 단계>
```

---

# 자동 작업에 적합한 태스크 형태

좋은 태스크:

```text
config 파일에 option X를 추가하고, docs/configuration.md와 테스트를 갱신해라.
```

좋지 않은 태스크:

```text
설정 시스템을 개선해라.
```

좋은 태스크는 다음을 가진다.

```text
범위
책임 영역
완료 기준
검증 방법
문서 갱신 여부
```

---

# 에이전트에게 줄 운영 지시문

프로젝트의 `AGENTS.md`나 상위 지침에 넣을 수 있는 문장이다.

```text
작업을 시작하면 먼저 AGENTS.md와 관련 docs를 읽는다.
문서에 없는 아키텍처, 의존성, 보안, 저장 포맷 결정을 임의로 하지 않는다.
구현 중 문서와 코드가 달라지면 같은 작업에서 맞춘다.
완료 전 관련 검증 명령을 실행한다.
검증하지 못한 항목은 숨기지 말고 이유와 남은 위험을 보고한다.
자동 검증이 불가능한 영역은 수동 검증 방법과 한계를 적는다.
작업이 길어지면 인계 요약을 남긴다.
```
