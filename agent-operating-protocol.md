# AI 에이전트 운영 계약

이 문서는 장기 작업에서 프로젝트가 보존해야 하는 변경 권한, 중단 조건, verification evidence, 문서 정합성, 인계(handoff) 계약을 정리한다.

coding, debugging, review 순서나 `skill`, model, subagent 운용법은 정하지 않는다. 그 실행 방법은 사용하는 agent와 methodology가 맡는다.

---

# 변경 권한

버그 분석, 실패 원인 조사, review 요청은 수정 요청으로 해석하지 않는다.

진단 결과는 증상, 재현 조건, evidence, 확실성을 구분한다.

```text
원래 증상과 재현 절차
재현에 사용한 입력, 명령, 환경
실패 결과와 artifact
확인된 원인 또는 아직 남은 가설
재현됨 / 유력하지만 미확정 / 재현 안 됨 / 환경상 건너뜀 / 현재 이미 해결됨
```

진단만 요청받았다면 기존 구현이나 추적되는 파일을 수정하지 않는다. 재현 test나 문서처럼 저장소에 남는 변경이 필요하면 먼저 변경 권한을 확인한다.

---

# 진행해도 되는 경우

다음 조건을 만족하면 에이전트가 사용자에게 매번 묻지 않고 진행해도 된다.

```text
요청 범위가 명확하다.
관련 문서에 책임 경계가 있다.
새 의존성을 추가하지 않는다.
공개 API나 저장 포맷을 바꾸지 않는다.
보안/개인정보 정책에 영향을 주지 않는다.
검증 방법이 있다.
```

---

# 반드시 멈춰야 하는 경우

다음 상황에서는 구현을 계속하지 말고 사용자에게 보고한다.

```text
문서에 없는 아키텍처 결정을 해야 한다.
새 의존성이 필요하다.
공개 API, 저장 포맷, 프로토콜, DB schema를 바꿔야 한다.
보안/개인정보/권한 정책이 바뀐다.
기존 전략과 충돌한다.
자동 검증이 불가능하고 수동 검증도 애매하다.
참고 프로젝트의 코드 표현을 가져오거나 라이선스가 불명확한 자료를 사용해야 한다.
요청을 만족하려면 범위를 크게 넓혀야 한다.
```

보고 형식:

```text
멈춘 이유:
  <무엇이 문서에 없거나 위험한가>

선택지:
  1. <안전한 선택>
  2. <범위가 넓은 선택>

추천:
  <추천과 이유>
```

---

# 검증 evidence

프로젝트의 `verification-matrix`나 동등한 문서를 검증 gate의 단일 출처로 사용한다. 이 문서는 모든 프로젝트에 같은 테스트 종류나 실행 순서를 강제하지 않는다.

완료 보고에는 적용되는 gate의 명령, 결과, artifact 접근 경로, 실행하지 못한 검증과 남은 위험을 적는다.

검증하지 못한 경우 실패가 아니라 정보다.

반드시 보고한다.

```text
실행하지 못한 검증:
  <명령>

이유:
  <환경, 시간, 외부 의존성>

대체 확인:
  <수동 확인 또는 낮은 단계 테스트>

남은 위험:
  <무엇이 아직 불확실한가>
```

---

# 문서 정합성 규칙

다음 중 하나라도 해당하면 문서를 갱신한다.

```text
새 기능이 추가됐다.
명령이 바뀌었다.
테스트 경로가 바뀌었다.
파일 구조가 바뀌었다.
아키텍처 경계가 바뀌었다.
한계나 수동 검증 방법이 새로 생겼다.
```

같은 계약 위반이 반복되고 프로젝트가 기계적으로 판정할 invariant를 정했다면 설명 문구를 계속 추가하지 않고 `verification-matrix`의 gate로 연결한다. gate에는 적용 조건, 실패 의미, 확인할 evidence와 현재 예외를 적는다.

판정 기준이 없거나 새 gate가 현재 완료 기준을 바꾸는데 변경 권한이 없다면 임의의 proxy gate를 만들지 않는다. 필요한 사용자 결정과 그전까지의 review 절차를 보고한다.

구현의 단일 출처와 그 동작을 설명하는 유지 문서가 서로 다른 저장소에 있을 수 있다.

이 경우 다음을 따른다.

```text
먼저 어느 저장소가 실제 동작의 단일 출처이고 어느 문서가 그 동작을 설명하는지 구분한다.
두 저장소가 모두 요청 범위에 있으면 구현과 문서를 함께 갱신하고 정합성을 검증한다.
문서가 요청 범위 밖이면 다른 저장소를 임의로 수정하지 않는다.
완료 보고에 저장소, 파일 경로, 바뀐 동작, 필요한 후속 작업을 구체적으로 남긴다.
외부 단일 출처에 접근할 수 없으면 정합성을 확인했다고 말하지 않고 확인하지 못한 범위와 남은 위험을 보고한다.
```

문서를 갱신하지 않는다면 이유를 완료 보고에 적는다.

---

# 완료 보고 형식

프로젝트에 `docs/pr-checklist.md`가 있으면 그 문서를 작업 완료 보고의 단일 출처로 따른다.

그 문서가 없을 때만 다음 최소 형식을 사용한다.

```text
완료:
  <무엇을 했는가>

변경:
  - <파일/영역>
  - <파일/영역>

검증:
  - <명령>: 통과
  - <명령>: 통과

문서:
  - <갱신한 문서>
  - 문서 변경 없음: <이유>

한계:
  - <없으면 없음>

후속:
  - <docs/implementation-plan.md의 관련 항목, 없으면 없음>
```

---

# Handoff 요약 형식

프로젝트는 장기 작업을 다른 session이나 agent가 이어받을 때 사용할 인계 위치와 형식을 정한다. 기존 issue, PR, 구현 계획, 작업 문서를 우선하고 인계만을 위한 새 문서를 기본으로 만들지 않는다. 다음 session이 현재 대화를 볼 수 없다면 채팅에만 남긴 요약은 인계로 보지 않는다.

```text
소유 계획/작업:
  <계획 문서, issue, PR 또는 작업 식별자>

기준 상태:
  <branch와 commit>

현재 목표:
  <무엇을 끝내려는가>

완료한 것:
  - <완료 항목>

남은 것:
  - <남은 항목>

중요한 결정:
  - <결정과 이유>

주의할 점:
  - <건드리면 안 되는 것>

검증 상태:
  - <통과한 명령>
  - <아직 못 한 명령>

다음 추천 작업:
  1. <다음 단계>
```

인계 기록은 첫 부분에서 자신이 속한 계획이나 작업을 식별해야 한다. 여러 장기 작업이 동시에 진행되면 하나의 공용 진행 기록에 섞지 않고 계획이나 작업별로 분리한다.

인계 기록의 완료 상태는 저장소 evidence보다 우선하지 않는다. 재개할 때 현재 branch, commit, working tree, test 또는 artifact와 맞지 않으면 충돌과 남은 위험을 보고한다. 대화 요약이나 todo만을 완료 상태의 단일 근거로 사용하지 않는다.
