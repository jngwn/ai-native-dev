# 참고 프로젝트 분석 템플릿

이 문서는 에이전틱 개발(agentic development) 프로젝트를 참고해 `ai-native-dev`의 현재 계약 체계에 누락이 있는지 점검하는 템플릿이다.

참고 프로젝트는 새 workflow의 공급원이 아니다. 목표는 "그 프로젝트처럼 만들기"나 좋은 작업 방법을 일반화하는 것이 아니다.

분석 요청은 변경 요청이 아니다. 일반화할 수 있고 failure mode를 줄인다는 사실만으로는 반영을 정당화하지 않는다. 반영할 가치가 있는 변경이 없으면 "반영할 변경 없음"을 명시한다.

이 저장소가 소유하는 범위는 다음 두 종류로 닫혀 있다.

```text
권한과 안전의 불변 조건:
  변경 권한, 사람이 결정할 경계, evidence와 남은 위험의 보고

프로젝트가 채워야 하는 계약의 틀:
  목표와 범위 밖, architecture와 책임 경계, artifact, verification gate, 인계(handoff) 기준
```

반영 제안은 다음 조건을 모두 만족해야 한다.

```text
보완할 기존 문서와 계약을 식별한다.
프로젝트가 채워야 하는 값, 경계 또는 evidence를 더 명확하게 한다.
에이전트의 coding, debugging, review, task dispatch 방법을 규정하지 않는다.
가능하면 기존 규칙을 대체하거나 단순화한다.
```

참고 프로젝트에 규칙이 존재하거나 잘 작동한다는 사실은 이 저장소의 실패 근거가 아니다. 대응하는 기존 계약이 없으면 기본적으로 소유 범위 밖으로 분류한다. 반복해서 확인된 고비용 실패 때문에 새로운 핵심 책임이 필요하다면 반영 제안이 아니라 저장소 역할 확장에 관한 사용자 결정으로 보고한다. 경계가 애매하면 반영하지 않는다.

참고 대상은 역할을 구분해서 본다.

```text
ground truth:
  구현이나 정책의 1차 기준이 되는 공개 명세, 공식 문서, 표준.

oracle:
  결과가 맞는지 비교하는 독립 구현이나 실행 결과. 코드 표현은 가져오지 않는다.

workload:
  프로젝트 안에서 돌려 보는 실제 사용 시나리오나 프로그램. 정답 기준이 아니라 압박 테스트다.

code reference:
  구조나 동작을 관찰할 수 있는 코드. 라이선스와 clean-room 경계 때문에 그대로 옮기지 않는다.
```

---

# 분석 대상

프로젝트:

- <path or repository URL>

분석 범위:

- <예: AGENTS.md>
- <예: docs/>
- <예: pull request template>
- <예: 결정 기록>

분석하지 않은 것:

- <예: product code>
- <예: 생성 산출물>
- <예: vendored dependencies>

분석 기준일:

- YYYY-MM-DD

---

# 프로젝트의 문서 구조

문서 진입점:

- <에이전트가 처음 읽는 문서>

주요 문서:

| 문서 | 역할 | 단일 출처 여부 |
| --- | --- | --- |
| <path> | <역할> | <예/아니오/불명확> |

좋은 점:

- <문서 구조가 에이전트 작업을 어떻게 안정화하는가>

불명확한 점:

- <문서 역할 충돌, 중복, 누락>

현재 계약과 대조:

- 기존 소유 문서와 계약: <path와 section / 없음>
- 분류: <이미 포함됨 / 구체적인 누락 / 소유 범위 밖>

---

# 운영 계약 대조

권한과 중단 조건:

- <프로젝트가 무엇을 허용하고 언제 사용자 결정을 요구하는가>

필요한 artifact와 evidence:

- <완료, 실패, 인계를 무엇으로 증명하는가>

agent 또는 methodology의 내부 전술:

- <작업 순서, skill, orchestration, debugging/review 방법>

현재 계약과 대조:

- 기존 소유 문서와 계약: <path와 section / 없음>
- 분류: <이미 포함됨 / 구체적인 누락 / 소유 범위 밖>
- 근거: <실제 사례 또는 재현 가능한 시나리오>

---

# 구현 계획 계약 대조

현재 목표와 검증할 사용자 결과가 있는가:

- <예/아니오>

단계별 완료 기준이 있는가:

- <예/아니오>

"아직 하지 않을 것"이 명시되어 있는가:

- <예/아니오>

구현 계획이 현재와 다음 작업을 안내하는가:

- <예/아니오>

상세 history가 계획 본문에 누적되어 있는가:

- <예/아니오, 상세 history의 실제 소유 위치>

현재 계약과 대조:

- 기존 소유 문서와 계약: <path와 section / 없음>
- 분류: <이미 포함됨 / 구체적인 누락 / 소유 범위 밖>
- 방법론이라 반영하지 않을 항목: <없으면 없음>

---

# 검증 계약 대조

검증 명령이 문서화되어 있는가:

- <예/아니오>

기능별 검증 기준이 있는가:

- <예/아니오>

실패 artifact가 있는가:

- <예/아니오>

수동 검증과 한계를 기록하는가:

- <예/아니오>

현재 계약과 대조:

- 기존 소유 문서와 계약: <path와 section / 없음>
- 분류: <이미 포함됨 / 구체적인 누락 / 소유 범위 밖>
- 누락된 프로젝트별 값, gate 또는 evidence: <없으면 없음>

---

# Reference 사용 계약 대조

ground truth가 있는가:

- <예/아니오>

oracle이 있는가:

- <예/아니오>

workload와 oracle을 구분하는가:

- <예/아니오>

code reference 사용 제한이 명확한가:

- <예/아니오>

구분 방식:

- <명세, 오라클, workload, code reference를 어떻게 구분하는가>

주의할 점:

- <라이선스, 코드 표현 복사 위험, 스택 고유 의존성>

현재 계약과 대조:

- 기존 소유 문서와 계약: <path와 section / 없음>
- 분류: <이미 포함됨 / 구체적인 누락 / 소유 범위 밖>

---

# 결정 기록 계약 대조

결정 기록이 있는가:

- <예/아니오>

기록하는 결정의 기준:

- <아키텍처, 의존성, 보안, 저장 포맷 등>

결정 변경 또는 현행화 방식:

- <과거 결정을 보존하는가>

현재 계약과 대조:

- 기존 소유 문서와 계약: <path와 section / 없음>
- 분류: <이미 포함됨 / 구체적인 누락 / 소유 범위 밖>

---

# 소유 범위 밖 요소

반영하지 않을 것:

| 요소 | 반영하지 않는 이유 |
| --- | --- |
| <언어/도구/파일명> | <프로젝트 고유 요소이기 때문> |
| <workflow 또는 orchestration> | <에이전트나 methodology의 내부 전술이기 때문> |

기존 계약과 대조할 수 있는 관찰:

| 관찰 | 기존 소유 문서와 계약 | 분류 |
| --- | --- | --- |
| <참고 프로젝트의 방식> | <path와 section / 없음> | <이미 포함됨 / 구체적인 누락 / 소유 범위 밖> |

---

# 반영 제안

즉시 반영할 변경:

- 기존 소유 문서와 계약: <path와 section>
- 확인된 누락과 근거: <실제 사례 또는 재현 가능한 시나리오>
- 대체하거나 단순화할 기존 규칙: <규칙 / 없음과 이유>
- 방법론이 아닌 이유: <프로젝트가 채워야 하는 값, 경계 또는 evidence>

반영할 변경 없음:

- <없으면 없음, 있으면 왜 반영할 변경이 없는지>

보류할 변경:

- <이유>

반영하지 않을 변경:

- <methodology, agent 내부 전술, 스택 고유 요소 또는 과도한 구조>

사용자 결정 필요:

- <새로운 핵심 책임 때문에 저장소 역할을 넓혀야 하는지, 없으면 없음>

---

# 요약

이 참고 프로젝트와 대조해 확인한 핵심:

```text
<한두 문장 요약>
```

이 저장소에 적용할 때의 원칙:

```text
<새 workflow를 일반화하지 않고 기존 계약의 누락만 보완한다>
```
