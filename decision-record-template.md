# 결정 기록과 RFC 템플릿

큰 프로젝트는 코드보다 결정이 더 오래 남는다.

AI 에이전트에게 장기 작업을 맡기려면 "왜 이렇게 했는가"를 남기는 형식이 필요하다.

---

# 언제 결정 기록을 남기는가

다음에 해당하면 기록한다.

```text
아키텍처 경계가 바뀐다.
새 런타임 의존성을 추가한다.
공개 API나 저장 포맷이 바뀐다.
성능/메모리 전략을 선택한다.
보안/권한/개인정보 정책을 정한다.
레퍼런스 프로젝트나 공개 명세를 기준으로 삼는다.
나중에 되돌리기 어려운 선택을 한다.
```

작은 구현 디테일은 기록하지 않아도 된다.

새 의존성은 `docs/project-rules.md`의 승인 정책을 먼저 따른다. 그중 runtime이나 장기 구조에 영향을 주는 선택만 결정 기록으로 남기고, 일회성 승인 내역을 모두 복사하지 않는다.

---

# decisions.md 형식

`docs/decisions.md`는 짧은 결정 로그다.

여러 단계나 책임 영역에 영향을 주는 보류 결정도 이 문서에서 관리한다. 단계 안에서만 필요한 질문은 `docs/implementation-plan.md`에 두고, `AGENTS.md`와 아키텍처 문서에서는 같은 내용을 복사하지 않고 해당 기록을 가리킨다.

```md
# 결정 기록

이 문서는 프로젝트의 중요한 결정과 이유를 보존하는 단일 출처다.

## D001: <제목>

상태: 결정됨 / 보류 / 폐기 / 현행화됨

날짜: YYYY-MM-DD

결정:

- <선택한 것>

이유:

- <왜 선택했는가>

대안:

- <대안 1>
- <대안 2>

영향:

- <아키텍처/테스트/UX/성능 영향>

재검토 조건:

- <언제 다시 볼 것인가>
```

---

# RFC 형식

결정이 크면 `docs/rfcs/0001-<topic>.md`로 따로 쓴다.

실험이나 POC를 포함한 RFC는 예상과 실제 결과를 구분한다. 결과가 나오기 전에는 `Outcome`을 비워둘 수 있지만, 최종 상태를 정할 때는 관찰 결과와 재검토 조건을 채운다.

```md
# RFC 0001: <제목>

상태: draft / accepted / rejected / superseded

## Context

왜 이 결정이 필요한가?

현재 문제가 무엇인가?

## Goals

- <목표 1>
- <목표 2>

## Non-goals

- <이번에 하지 않을 것>
- <범위 밖>

## Options

### Option A: <이름>

장점:

- <장점>

단점:

- <단점>

### Option B: <이름>

장점:

- <장점>

단점:

- <단점>

## Decision

선택:

- <선택한 옵션>

이유:

- <근거>

## Consequences

좋은 영향:

- <영향>

나쁜 영향:

- <영향>

## Verification

이 결정이 맞게 구현됐는지 확인하는 방법:

- <테스트>
- <artifact>
- <수동 검증>

## Outcome

실험이나 POC가 없으면 해당 없음으로 적는다.

예상:

- <예상한 결과>

관찰 결과:

- <실제 결과와 artifact>

Gate 결과:

- GO / NO-GO / 사용자 예외 승인 / 해당 없음

최종 처리:

- <진행, 폐기, 부분 채택>
- <gate와 다른 결론이면 사용자 승인 이유와 남은 위험>

재검토 조건:

- <어떤 전제가 바뀌면 다시 검토하는가>

## Migration

기존 코드나 문서에서 바뀌는 것:

- <변경>

## Open Questions

- <남은 질문>
```

---

# 레퍼런스 사용 기록

다른 프로젝트를 참고할 때는 더 엄격히 쓴다.

```md
## Reference Basis

참고한 것:

- <공개 명세>
- <제품 동작>
- <문서>

허용한 사용:

- 동작 비교
- 공개 API/프로토콜 이해
- UX 관찰

금지한 사용:

- 코드 복사
- 자료구조/함수 분해 복제
- 라이선스 위험 소스 차용

선택한 동작:

- <레퍼런스 간 차이가 있을 때 무엇을 택했는가>

이유:

- <왜 그 동작이 프로젝트에 맞는가>
```

---

# 현행화 규칙

결정이 바뀌면 과거 기록을 지우지 않는다.

나쁜 방식:

```text
과거 결정을 삭제하고 현재 상태만 남긴다.
```

좋은 방식:

```md
## D001: 저장 포맷

상태: 현행화됨

원래 결정:

- JSON 파일 하나에 저장한다.

현행:

- SQLite에 저장한다.

변경 이유:

- 대량 데이터에서 부분 업데이트와 인덱싱이 필요해졌다.

관련 PR/RFC:

- RFC 0007
```

AI 에이전트는 과거 결정을 보고 현재와 다르다고 판단하면 임의로 정리하지 말고 "현행화 필요"로 보고한다.
