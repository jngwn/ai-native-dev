# 구현 계획 템플릿

이 문서는 프로젝트가 정한 현재와 다음 범위, 선행 조건, gate, evidence를 기록하는 계획 템플릿이다.

구현 계획은 보편적인 작업 순서를 가르치지 않는다. 프로젝트가 선택한 순서와 완료 조건을 다음 agent나 session도 같은 의미로 읽게 한다.

`project-documents-template.md`의 구현 계획은 최소형이다. 여러 단계의 의존 관계, 별도 gate, 불확실한 결정 기준이 필요하면 이 템플릿으로 `docs/implementation-plan.md`를 확장하고, 프로젝트 안에서는 두 형식을 독립된 규칙으로 유지하지 않는다.

---

# 기본 구조

````md
# 구현 계획

## 목표

<제품 또는 기능이 사용자에게 제공하려는 결과>

## 현재 검증할 결과

```text
<입력>
-> <핵심 처리>
-> <출력>
-> <artifact, 필요한 경우>
```

## 원칙

- 단계의 수, 이름, 순서와 크기는 프로젝트의 의존 관계와 검증 조건이 정한다.
- 각 단계는 프로젝트가 정한 관찰 가능한 결과를 가진다.
- 테스트 출력만으로 결과를 설명하기 어려운 단계는 필요한 artifact와 접근 경로를 정한다.
- 각 단계는 완료 기준과 "아직 하지 않을 것"을 가진다.
- 문서와 구현이 어긋나면 구현 완료로 보지 않는다.
- 이 문서는 특정 PR 크기, 작업 session 길이, coding이나 review 순서를 강제하지 않는다.
````

---

# 단계 템플릿

각 단계는 아래 형식을 쓴다.

```md
## N단계: <단계 이름>

### 목표

- <이 단계가 끝나면 가능해야 하는 것>

### 구현

- <파일/모듈/책임 영역>

### 테스트

- <테스트 또는 검증 명령>

### 산출물

- <artifact path, 없으면 없음>
- <snapshot/log/report, 필요한 경우>

### 문서

- <갱신할 문서와 내용, 없으면 이유>

### 완료 기준

- <통과할 명령, 관찰 가능한 결과 또는 artifact 1>
- <통과할 명령, 관찰 가능한 결과 또는 artifact 2>

### 아직 하지 않는다

- <범위 밖 1>
- <범위 밖 2>

### 사용자 결정 필요

이 단계 안에서만 필요한 질문을 적는다. 여러 단계나 책임 영역에 영향을 주는 보류 결정은 `docs/decisions.md`에 기록하고 여기서는 해당 항목을 가리킨다.

- <단계 한정 질문, 없으면 없음>
```

---

# 프로젝트별 단계와 gate

여러 단계가 서로 의존하거나 독립적으로 완료될 수 있을 때만 단계 관계를 명시한다.

| 단계 또는 milestone | 프로젝트가 정한 범위 | 선행 조건 | 완료 gate | evidence | 범위 밖 |
| --- | --- | --- | --- | --- | --- |
| <이름> | <포함할 책임 영역> | <없음 또는 필요한 상태> | <통과 조건> | <test, artifact 또는 관찰 결과> | <다음 단계로 남길 것> |

단계를 나누는 방식은 프로젝트가 정한다. 별도 단계가 계약의 소유권이나 검증 가능성을 높이지 않으면 형식에 맞추기 위해 쪼개지 않는다.

---

# 불확실한 작업의 결정 gate

성능 최적화, architecture spike, migration POC, dependency 평가처럼 결과를 구현 전에 확정할 수 없는 단계는 아래 선택형 형식을 사용한다. 모든 단계에 요구하지 않는다.

```text
가설:
  <왜 이 접근이 문제를 해결한다고 보는가>

baseline:
  <비교 기준, 실행 전제, 결과 무효화 조건>

결정 gate:
  GO:
    <다음 범위로 진행할 조건>
  NO-GO:
    <이 접근을 종료할 조건>

결과 artifact:
  <측정, 비교 결과, 로그, 보고서>

NO-GO 이후:
  <보존할 근거와 폐기할 구현 범위>

재시도 조건:
  <어떤 전제, workload, 환경이 바뀌어야 다시 시도하는가>
```

규칙:

```text
결정 gate와 결과 유효성 조건은 구현 또는 측정 전에 정한다.
정확성 회귀와 목표 효과 미달은 별도 gate로 판단한다.
환경이나 측정 전제가 충족되지 않으면 GO/NO-GO가 아니라 결과 무효로 기록한다.
NO-GO도 완료된 결과다. 근거 artifact와 재시도 조건을 남기고 같은 접근을 열린 작업처럼 유지하지 않는다.
여러 단계에 영향을 주는 최종 결정은 docs/decisions.md나 관련 RFC에 기록하고 계획에는 링크만 둔다.
```

---

# 구현 계획 lifecycle

구현 계획은 현재와 다음 작업의 단일 출처이며 changelog가 아니다.

규칙:

```text
완료한 단계는 결과, 통과한 gate, 남은 한계만 간결하게 남긴다.
파일별 변경 내역, commit 나열, review 대화는 PR 보고나 기존 인계 기록으로 보내고 계획에는 링크만 둔다.
계획이 바뀌면 기존 방향을 조용히 지우지 말고 대체된 항목, 현재 방향, 변경 이유를 표시한다.
서로 모순되는 이전 계획을 본문에 계속 쌓아 두지 않는다.
별도 archive 문서는 기본으로 만들지 않는다. 접근 가능한 PR, 결정 기록, 인계 기록이 상세 이력을 보존하면 그것을 사용한다.
```

---

# 계획 리뷰 체크리스트

이 계획을 사용할 때 아래를 확인한다.

```text
현재 목표와 관찰 가능한 결과가 명확한가?
단계가 있다면 그 수와 순서가 프로젝트의 실제 의존 관계를 반영하는가?
각 단계의 범위, 선행 조건, 완료 gate와 evidence가 명확한가?
현재 범위에서 하지 않을 것이 드러나는가?
완료한 단계가 결과, 통과 gate, 남은 한계만 남기고 있는가?
검증 명령이 실제로 존재하거나 만들 계획이 있는가?
자동 검증이 어려운 부분의 수동 검증 방법이 있는가?
관련 문서의 갱신 범위 또는 변경하지 않는 이유가 있는가?
사용자 결정 필요 항목이 숨겨져 있지 않은가?
특정 PR 크기나 작업 순서를 프로젝트 근거 없이 강제하지 않는가?
```
