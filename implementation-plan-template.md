# 구현 계획 템플릿

이 문서는 큰 기능의 단계별 목표, 범위, gate, evidence를 기록하는 계획 템플릿이다.

좋은 구현 계획은 작업 목록이 아니라 검증 가능한 경로다.

`project-documents-template.md`의 구현 계획은 최소 baseline이다. 큰 phase, 여러 micro-slice, 선행/종료 gate가 필요하면 이 템플릿으로 `docs/implementation-plan.md`를 확장하고, 프로젝트 안에서는 두 형식을 독립된 규칙으로 유지하지 않는다.

---

# 기본 구조

````md
# 구현 계획

## 목표

<제품 또는 기능이 사용자에게 제공하려는 결과>

## 첫 번째 세로 슬라이스

```text
<입력>
-> <핵심 처리>
-> <출력>
-> <artifact, 필요한 경우>
```

## 원칙

- 각 단계는 프로젝트가 정한 관찰 가능한 결과를 가진다.
- 테스트 출력만으로 결과를 설명하기 어려운 단계는 필요한 artifact와 접근 경로를 정한다.
- 각 단계는 완료 기준과 "아직 하지 않을 것"을 가진다.
- 문서와 구현이 어긋나면 구현 완료로 보지 않는다.
- 큰 phase를 나눠야 하면 각 slice의 범위와 독립 검증, 종료 gate를 정한다.
````

---

# 단계 템플릿

각 단계는 아래 형식을 쓴다.

```md
## N단계: <단계 이름>

### 목표

- <이 단계가 끝나면 가능해야 하는 것>

### 구현

- <파일/모듈/책임 영역>

### 테스트

- <테스트 또는 검증 명령>

### 산출물

- <artifact path, 없으면 없음>
- <snapshot/log/report, 필요한 경우>

### 문서

- <갱신할 문서와 내용, 없으면 이유>

### 완료 기준

- <통과할 명령, 관찰 가능한 결과 또는 artifact 1>
- <통과할 명령, 관찰 가능한 결과 또는 artifact 2>

### 아직 하지 않는다

- <범위 밖 1>
- <범위 밖 2>

### 사용자 결정 필요

이 단계 안에서만 필요한 질문을 적는다. 여러 단계나 책임 영역에 영향을 주는 보류 결정은 `docs/decisions.md`에 기록하고 여기서는 해당 항목을 가리킨다.

- <단계 한정 질문, 없으면 없음>
```

---

# Phase와 micro-slice

큰 프로젝트에서는 phase와 micro-slice를 구분한다.

```text
Phase:
  제품이나 아키텍처 관점의 큰 이정표.

Micro-slice:
  하나의 PR이나 짧은 작업 세션에서 끝낼 수 있는 검증 가능한 조각.
```

phase 하나를 한 번에 구현하는 것을 기본값으로 두지 않는다. phase 안의 각 micro-slice는 아래를 가져야 한다.

```text
목표:
  <이 slice에서 닫을 작은 계약>

선행 gate:
  <이전 slice에서 다시 확인할 검증>

구현:
  <파일/모듈/책임 영역>

검증:
  <이 slice의 완료를 증명할 테스트, fixture, smoke 또는 수동 artifact>

문서:
  <갱신할 문서와 내용, 없으면 이유>

종료 gate:
  <이 slice가 끝났다고 말할 수 있는 조건>

아직 하지 않는다:
  <다음 slice로 넘길 범위>
```

규칙:

```text
새 capability, transport, 저장 포맷, 권한 경계, CLI 명령, UI bridge는 micro-slice 안에서 parser, 문서, 검증이 함께 닫혀야 한다.
자동화가 어려운 영역은 수동 검증 evidence와 자동화 가능한 후속 gate를 적는다.
이전 micro-slice의 종료 gate가 깨지면 다음 slice로 넘어가지 않는다.
여러 micro-slice를 묶을 수는 있지만, 왜 함께 묶어도 리뷰와 rollback이 안전한지 적는다.
```

---

# 불확실한 작업의 결정 gate

성능 최적화, architecture spike, migration POC, dependency 평가처럼 결과를 구현 전에 확정할 수 없는 micro-slice는 아래 선택형 형식을 사용한다. 모든 micro-slice에 요구하지 않는다.

```text
가설:
  <왜 이 접근이 문제를 해결한다고 보는가>

baseline:
  <비교 기준, 실행 전제, 결과 무효화 조건>

결정 gate:
  GO:
    <다음 micro-slice로 진행할 조건>
  NO-GO:
    <이 접근을 종료할 조건>

결과 artifact:
  <측정, 비교 결과, 로그, 보고서>

NO-GO 이후:
  <보존할 근거와 폐기할 구현 범위>

재시도 조건:
  <어떤 전제, workload, 환경이 바뀌어야 다시 시도하는가>
```

규칙:

```text
결정 gate와 결과 유효성 조건은 구현 또는 측정 전에 정한다.
정확성 회귀와 목표 효과 미달은 별도 gate로 판단한다.
환경이나 측정 전제가 충족되지 않으면 GO/NO-GO가 아니라 결과 무효로 기록한다.
NO-GO도 완료된 결과다. 근거 artifact와 재시도 조건을 남기고 같은 접근을 열린 작업처럼 유지하지 않는다.
여러 단계에 영향을 주는 최종 결정은 docs/decisions.md나 관련 RFC에 기록하고 계획에는 링크만 둔다.
```

---

# 구현 계획 lifecycle

구현 계획은 현재와 다음 작업의 단일 출처이며 changelog가 아니다.

규칙:

```text
완료한 micro-slice는 결과, 통과한 gate, 남은 한계만 간결하게 남긴다.
파일별 변경 내역, commit 나열, review 대화는 PR 보고나 기존 인계 기록으로 보내고 계획에는 링크만 둔다.
계획이 바뀌면 기존 방향을 조용히 지우지 말고 대체된 항목, 현재 방향, 변경 이유를 표시한다.
서로 모순되는 이전 계획을 본문에 계속 쌓아 두지 않는다.
별도 archive 문서는 기본으로 만들지 않는다. 접근 가능한 PR, 결정 기록, 인계 기록이 상세 이력을 보존하면 그것을 사용한다.
```

---

# 계획 리뷰 체크리스트

구현 전에 아래를 확인한다.

```text
첫 번째 세로 슬라이스가 너무 크지 않은가?
각 단계가 독립적으로 검증 가능한가?
큰 phase가 있다면 micro-slice로 나뉘어 있는가?
각 micro-slice가 선행 gate와 종료 gate를 가지는가?
각 단계에 "아직 하지 않는다"가 있는가?
완료한 단계가 결과, 통과 gate, 남은 한계만 남기고 있는가?
검증 명령이 실제로 존재하거나 만들 계획이 있는가?
자동 검증이 어려운 부분의 수동 검증 방법이 있는가?
각 단계와 micro-slice에 문서 갱신 범위 또는 변경하지 않는 이유가 있는가?
사용자 결정 필요 항목이 숨겨져 있지 않은가?
새 의존성이 단계 초반에 불필요하게 들어오지 않는가?
```

---

# 나쁜 구현 계획의 신호

```text
1단계부터 전체 기능을 만든다.
"리팩토링"이 독립 단계로 크게 잡혀 있다.
phase를 하나의 거대한 PR처럼 다룬다.
이전 gate가 깨졌는데 다음 slice로 넘어간다.
완료한 단계에 파일별 변경 내역과 review 대화를 계속 누적한다.
대체된 계획과 현재 계획이 구분 없이 함께 남아 있다.
아직 하지 않을 것이 없다.
사용자 결정 필요 항목이 없다.
완료 기준이 "구현 완료"라고만 되어 있다.
```

이런 계획은 구현 전에 다시 쪼갠다.
