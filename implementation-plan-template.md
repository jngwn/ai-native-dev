# 구현 계획 템플릿

이 문서는 AI 에이전트가 큰 기능을 작게 쪼개서 안전하게 구현하도록 만드는 계획 템플릿이다.

좋은 구현 계획은 작업 목록이 아니라 검증 가능한 경로다.

---

# 기본 구조

````md
# 구현 계획

## 목표

<제품 또는 기능이 사용자에게 제공하려는 결과>

## 첫 번째 세로 슬라이스

```text
<입력>
-> <핵심 처리>
-> <출력>
-> <artifact>
```

## 원칙

- 가장 작은 end-to-end 경로를 먼저 만든다.
- 실패 원인을 볼 수 있는 artifact를 먼저 만든다.
- 각 단계는 완료 기준과 "아직 하지 않을 것"을 가진다.
- 문서와 구현이 어긋나면 구현 완료로 보지 않는다.
- 큰 phase는 여러 micro-slice로 나누고, 각 slice는 독립 검증과 종료 gate를 가진다.
````

---

# 단계 템플릿

각 단계는 아래 형식을 쓴다.

```md
## N단계: <단계 이름>

### 목표

- <이 단계가 끝나면 가능해야 하는 것>

### 구현

- <파일/모듈/책임 영역>

### 테스트

- <테스트 또는 검증 명령>

### 산출물

- <artifact path>
- <snapshot/log/report>

### 완료 기준

- <기준 1>
- <기준 2>

### 아직 하지 않는다

- <범위 밖 1>
- <범위 밖 2>

### 사용자 결정 필요

- <없으면 없음>
```

---

# Phase와 micro-slice

큰 프로젝트에서는 phase와 micro-slice를 구분한다.

```text
Phase:
  제품이나 아키텍처 관점의 큰 이정표.

Micro-slice:
  하나의 PR이나 짧은 작업 세션에서 끝낼 수 있는 검증 가능한 조각.
```

phase 하나를 한 번에 구현하는 것을 기본값으로 두지 않는다. phase 안의 각 micro-slice는 아래를 가져야 한다.

```text
목표:
  <이 slice에서 닫을 작은 계약>

선행 gate:
  <이전 slice에서 다시 확인할 검증>

구현:
  <파일/모듈/책임 영역>

검증:
  <먼저 실패시키거나 새로 추가할 테스트, fixture, smoke, 수동 artifact>

종료 gate:
  <이 slice가 끝났다고 말할 수 있는 조건>

아직 하지 않는다:
  <다음 slice로 넘길 범위>
```

규칙:

```text
새 capability, transport, 저장 포맷, 권한 경계, CLI 명령, UI bridge는 micro-slice 안에서 parser, 문서, 검증이 함께 닫혀야 한다.
자동화가 어려운 영역은 수동 artifact를 먼저 남기고, 자동화 가능한 최소 계약을 후속 gate로 적는다.
이전 micro-slice의 종료 gate가 깨지면 다음 slice로 넘어가지 않는다.
여러 micro-slice를 묶을 수는 있지만, 왜 함께 묶어도 리뷰와 rollback이 안전한지 적는다.
```

---

# 추천 단계 구성

## 1단계: 문서와 책임 경계 고정

목표:

```text
에이전트가 어디에 무엇을 구현해야 하는지 알게 한다.
```

구현:

```text
AGENTS.md
docs/project-rules.md
docs/architecture.md
docs/verification-matrix.md
```

완료 기준:

```text
각 문서의 단일 출처 역할이 명확하다.
사용자 결정 필요 항목이 분리되어 있다.
초기 세로 슬라이스가 정의되어 있다.
```

아직 하지 않는다:

```text
제품 코드 작성
프레임워크 설치
디자인 시스템 구축
```

## 2단계: 최소 skeleton과 검증 명령

목표:

```text
프로젝트가 빌드되고 테스트 명령이 존재한다.
```

구현:

```text
최소 소스 구조
빌드 설정
테스트 runner
포맷/린트 명령
```

완료 기준:

```text
빌드 명령 통과
빈 테스트 또는 smoke test 통과
verification-matrix에 명령 기록
```

아직 하지 않는다:

```text
실제 기능 구현
복잡한 추상화
배포 자동화
```

## 3단계: 실패 산출물 먼저 만들기

이 단계는 외부 I/O, UI, E2E, 환경 의존 동작, 성능 측정처럼 테스트 실패 출력만으로 원인을 좁히기 어려운 경우에 적용한다. 순수 로직처럼 기존 테스트 출력으로 실패를 충분히 설명할 수 있으면 별도 artifact 단계를 생략하고 이유를 계획에 적는다.

목표:

```text
실패했을 때 원인을 볼 수 있는 자료가 남는다.
```

예:

```text
CLI라면 stdout/stderr fixture
웹앱이라면 screenshot
서버라면 request/response log
컴파일러라면 input/output golden
데스크톱 앱이라면 smoke summary와 screenshot
```

완료 기준:

```text
테스트 실패 시 artifact가 남는다.
artifact 위치가 문서화되어 있다.
민감정보가 들어가지 않는다.
```

## 4단계: 첫 번째 end-to-end 경로

목표:

```text
사용자가 기대하는 가장 작은 작업 흐름이 끝까지 돈다.
```

예:

```text
파일 입력 -> 파싱 -> 변환 -> 출력 파일
질문 입력 -> 검색 -> 결과 목록
버튼 클릭 -> 로컬 처리 -> 화면 갱신
```

완료 기준:

```text
통합/E2E 테스트 통과
실패 artifact 생성
문서의 첫 번째 세로 슬라이스와 일치
```

아직 하지 않는다:

```text
전체 기능
최적화
사용자 설정 UI
플러그인
멀티 플랫폼
```

## 5단계: 반복 확장

목표:

```text
각 기능을 별도 작은 PR/작업 단위로 확장한다.
```

규칙:

```text
새 기능마다 verification-matrix 갱신
새 결정마다 decisions/RFC 갱신
새 한계마다 PR 체크리스트에 기록
```

---

# 계획 리뷰 체크리스트

구현 전에 아래를 확인한다.

```text
첫 번째 세로 슬라이스가 너무 크지 않은가?
각 단계가 독립적으로 검증 가능한가?
큰 phase가 있다면 micro-slice로 나뉘어 있는가?
각 micro-slice가 선행 gate와 종료 gate를 가지는가?
각 단계에 "아직 하지 않는다"가 있는가?
검증 명령이 실제로 존재하거나 만들 계획이 있는가?
자동 검증이 어려운 부분의 수동 검증 방법이 있는가?
사용자 결정 필요 항목이 숨겨져 있지 않은가?
새 의존성이 단계 초반에 불필요하게 들어오지 않는가?
```

---

# 나쁜 구현 계획의 신호

```text
1단계부터 전체 기능을 만든다.
테스트가 마지막 단계에만 있다.
"리팩토링"이 독립 단계로 크게 잡혀 있다.
phase를 하나의 거대한 PR처럼 다룬다.
이전 gate가 깨졌는데 다음 slice로 넘어간다.
아직 하지 않을 것이 없다.
사용자 결정 필요 항목이 없다.
완료 기준이 "구현 완료"라고만 되어 있다.
```

이런 계획은 구현 전에 다시 쪼갠다.
